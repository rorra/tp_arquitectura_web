.container
  .row.justify-content-center
    .col-md-8
      .card.my-4
        .card-body
          %h1.h3.mb-4= t('devise.mailer.password_change.greeting', recipient: @resource.email)
          %p= t('devise.mailer.password_change.message')
